import { createTheme } from '@material-ui/core/styles';

const headlineTheme = createTheme({
    typography: {
        "fontFamily": `"Cairo"`,
        "fontSize": 24,
        "fontWeightMedium": 500
    }
});

export default headlineTheme;